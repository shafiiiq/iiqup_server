const RECENT_LIMIT = 100;
const FILTERED_LIMIT = 500;
const HISTORY_LIMIT = 20;

const MOBILIZATION_ACTIONS = {
  MOBILIZED: 'mobilized',
  DEMOBILIZED: 'demobilized',
  STATUS_CHANGED: 'status_changed',
};

module.exports = {
  RECENT_LIMIT,
  FILTERED_LIMIT,
  HISTORY_LIMIT,
  MOBILIZATION_ACTIONS,
};
