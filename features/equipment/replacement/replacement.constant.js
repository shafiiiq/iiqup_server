const RECENT_LIMIT = 100;
const FILTERED_LIMIT = 500;

const REPLACEMENT_TYPES = {
  OPERATOR: 'operator',
  EQUIPMENT: 'equipment',
};

module.exports = {
  RECENT_LIMIT,
  FILTERED_LIMIT,
  REPLACEMENT_TYPES,
};
